/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['my.geva.co.il', 'via.placeholder.com'], // Add the external domain here
    },
};

export default nextConfig;
